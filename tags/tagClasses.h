#pragma once

// just a file to put all tag class includes into one
// mostly just used by the TagLoader. Don't use this file if you only need one tag class

// This doesn't really work, since multiple tags use the same struct names
// Unless prefixes are used for each tag type, this will just end up in a giant mess

//#include "xml/generated/shdv.h"
#include "xml/generated/sbsp.h"
//#include "xml/generated/mode.h"
